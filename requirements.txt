# Front end only. The package itself imports nothing outside the standard
# library, which check-sources.sh enforces on every push.
#
# Requires Python 3.10 or newer (a Streamlit requirement, not ours).
streamlit>=1.30
pytest>=7.0
